/*
 * EarnLaPage Messages
 *
 * This contains all the text for the EarnLaPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EarnLaPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EarnLaPage container!',
  },
});
