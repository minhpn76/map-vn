/*
 * LaHistoryPage Messages
 *
 * This contains all the text for the LaHistoryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LaHistoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LaHistoryPage container!',
  },
});
