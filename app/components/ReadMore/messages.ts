/*
 * ReadMore Messages
 *
 * This contains all the text for the ReadMore component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReadMore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReadMore component!',
  },
});
