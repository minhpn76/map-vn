/*
 * EarnTrongDong Messages
 *
 * This contains all the text for the EarnTrongDong component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EarnTrongDong';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EarnTrongDong component!',
  },
});
