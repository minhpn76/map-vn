/*
 * EarnLaHoa Messages
 *
 * This contains all the text for the EarnLaHoa component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EarnLaHoa';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EarnLaHoa component!',
  },
});
