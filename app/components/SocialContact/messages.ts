/*
 * SocialContact Messages
 *
 * This contains all the text for the SocialContact component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SocialContact';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialContact component!',
  },
});
